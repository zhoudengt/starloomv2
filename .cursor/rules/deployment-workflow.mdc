---
description: 代码必须本地改、提交仓库；生产仅从仓库拉取再构建发布；禁止服务器上直接改业务代码
alwaysApply: true
---

# StarLoom：代码变更与发布流程（强制）

## 唯一正确路径

1. **在本机（或团队约定的开发机）工作区修改代码** — 使用 Git 管理，与 `http://localhost:5173` + 本机后端联调（见 `.cursorrules` 浏览器门禁）。
2. **提交并推送到远程仓库** — 清晰 commit message；按团队流程走 PR/合并。
3. **生产环境**：在服务器上 **`git pull`（或等价方式拉取已合并的指定分支/标签）** 更新到目标目录（见仓库根目录 `DEPLOY.md` 中的路径与分支约定）。
4. **再执行构建与发布** — 例如前端 `npm ci && npm run build`、镜像构建、`podman compose` / `docker compose` up、迁移脚本等，**以 `DEPLOY.md` 与运维文档为准**。

## 禁止

- **禁止在生产服务器上直接修改业务源码**（例如 SSH 上去 `vim`/`sed` 改 `backend/app`、`frontend/src`，或未入库的临时覆盖）。
- **禁止**把「只在服务器上改了一版能跑」当作正式交付；未进仓库的改动无法追溯、无法回滚、易被下次部署覆盖。
- Cursor Agent **不得**建议或协助「登录服务器直接改代码」作为常规方案；若用户坚持紧急热修，仍应强调事后**补提交到仓库**并与线上一致。

## 与现有文档的关系

- 服务器操作顺序、Podman、路径、环境变量：见 **`DEPLOY.md`**。
- 本机运行时 Python/venv：见 **`starloom-local-runtime.mdc`**。
