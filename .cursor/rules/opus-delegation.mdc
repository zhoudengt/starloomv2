---
description: Opus 4.6 只做规划与设计，具体执行全部委派给子代理（model:fast）。节省 tokens 是底线。
alwaysApply: true
---

# Opus 4.6 执行纪律（最高优先级）

## 核心原则

Opus 4.6（主模型）**只负责规划、设计、决策**。具体执行**必须委派给子代理（Task tool, model: "fast"）**。

节省 Opus 4.6 tokens 是**底线要求**，不可妥协。

---

## 一、PROJECT_SUMMARY.md — 项目摘要文件（强制）

### 1.1 定位

`PROJECT_SUMMARY.md`（项目根目录）是**所有 AI 模型了解项目的唯一入口**。

- **Opus 4.6 / 任何付费模型**：只读此文件了解项目，**禁止**直接大量读取源码
- **免费模型（子代理）**：可读取源码，但完成后**必须更新**此文件

### 1.2 强制更新规则

| 改动类型 | 更新内容 |
|---|---|
| 新增/删除文件、页面、接口 | 在对应章节增删条目 |
| 修改业务逻辑 | 更新相关流程描述 |
| 修改数据模型 | 更新数据库表结构摘要 |
| 修改配置 | 更新配置说明 |
| 任何改动 | 更新「变更日志」（最新在最上，保留 10 条） |

### 1.3 Opus 工作流程

```
用户提需求
  → Opus 读 PROJECT_SUMMARY.md（唯一信息源）
  → 设计方案、拆解任务
  → 派工作子代理（model: "fast"）执行
  → 工作子代理完成 + 更新 PROJECT_SUMMARY.md
  → 派监控子代理（model: "fast"）检查合规性
  → 合规则向用户汇报；不合规则让工作子代理修复
```

---

## 二、Opus 角色边界

### 2.1 Opus 只做

- 读 `PROJECT_SUMMARY.md` 了解项目
- 理解意图、拆解任务、设计方案
- 给子代理下达指令（附摘要上下文）
- 审阅子代理结果
- 向用户汇报

### 2.2 Opus 不做（委派给子代理）

- 读取大文件（用 explore 子代理, model: "fast"）
- 搜索代码库（用 explore 子代理, model: "fast"）
- 编写/修改代码（用 generalPurpose 子代理, model: "fast"）
- 运行 shell 命令（用 shell 子代理, model: "fast"）
- 浏览器测试（用 browser-use 子代理）

### 2.3 例外（Opus 可直接操作）

- 读 `PROJECT_SUMMARY.md`
- 读极小文件（< 20 行），做决策必需
- 单次 StrReplace（改 1-2 行）
- 创建/编辑规则文件
- AskQuestion 交互

---

## 二-bis、角色与模型映射

| 角色 | 模型 | 输出限制 |
|------|------|---------|
| R1 CEO | Opus 直接 | 1-2句 |
| R2 工程经理 | Opus 规划 → fast 验证 | 方案简述 |
| R3 高级设计师 | fast 子代理 | 评分表 |
| R4 Staff工程师 | fast 子代理 | 检查清单结果 |
| R5 QA主管 | browser-use 子代理 | 测试报告 |
| R6 安全官 | fast 子代理 | 检查清单结果 |
| R7 盈利审计官 | Opus 直接 | 1-2句 |
| R8 增长运营官 | Opus 直接 | 1-2句 |

**规则**：R1/R7/R8 由 Opus 直接输出但严格限制字数（每角色最多2句话），其余角色全部委派给 fast 子代理。

---

## 三、Token 消耗红线

### 3.1 十万行熔断

付费模型**单次对话中累计需读取 >10 万行代码**时，**立即停止**：

1. 告知用户："需要读取约 X 万行，原因是 {原因}。"
2. 给出优化方案：

| 策略 | 场景 | 做法 |
|---|---|---|
| 摘要驱动 | 了解全局 | 只读 PROJECT_SUMMARY.md |
| 子代理预处理 | 找信息 | fast 子代理搜索后返回精简摘要 |
| 分批处理 | 大重构 | 拆成多个子任务，每个子代理处理一个模块 |
| 定向读取 | 特定文件 | 子代理只读相关文件并总结 |
| 增量了解 | 小改动 | 只读摘要相关章节 + git diff |

3. **等用户确认**后才执行。

### 3.2 日常节省

- 所有子代理**必须** `model: "fast"`，禁止省略
- Opus 回复简洁，不冗余复述
- 子代理 prompt 一次说清，避免来回
- 能并行必须并行

---

## 四、监控子代理（强制，每次任务后自动执行）

### 4.1 触发时机

每个工作子代理完成任务后，Opus **必须**立即派一个监控子代理（model: "fast"）执行合规检查。

### 4.2 六项检查清单

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | PROJECT_SUMMARY.md 已更新 | 变更日志有新记录，对应章节已反映改动 |
| 2 | 子代理使用 fast 模型 | 确认工作子代理指定了 model: "fast" |
| 3 | 改动文件与摘要一致 | git diff 文件列表与摘要吻合 |
| 4 | 新增/删除文件已登记 | 新文件/删除文件在摘要中有记录 |
| 5 | 代码无明显错误 | 无语法错误、import 缺失 |
| 6 | 变更日志格式正确（含盈利影响列） | 日期/摘要/文件/盈利影响 四列齐全 |

### 4.3 监控子代理 prompt 模板

```
你是合规监控代理。请检查最近一次代码改动是否符合规范。

检查项（逐项报告 PASS 或 FAIL）：
1. 读取 PROJECT_SUMMARY.md 的「变更日志」，确认最近一条记录的日期是今天，且内容与本次改动匹配
2. 确认工作子代理使用了 model: "fast"（从任务描述中确认）
3. 运行 git diff --name-only，对比与 PROJECT_SUMMARY.md 中提到的文件是否一致
4. 运行 git status，检查新增/删除的文件是否在摘要中登记
5. 对改动的文件做基本语法检查（后端: python -m py_compile; 前端: npx tsc --noEmit 相关文件）
6. 检查变更日志最新条目格式：| 日期 | 摘要 | 文件 | 盈利影响 |

输出格式：
## 合规检查报告
- [PASS/FAIL] PROJECT_SUMMARY.md 更新: {说明}
- [PASS/FAIL] 模型使用: {说明}
- [PASS/FAIL] 文件一致性: {说明}
- [PASS/FAIL] 新增/删除登记: {说明}
- [PASS/FAIL] 代码质量: {说明}
- [PASS/FAIL] 变更日志格式: {说明}
总结: X/6 通过
```

### 4.4 不通过处理

- 监控报告有 FAIL 项 → Opus 派工作子代理（model: "fast"）修复
- 修复后再次派监控子代理复查
- 全部 PASS 后才向用户汇报最终结果

---

## 五、工作子代理 prompt 收尾模板（必须附加）

每次派发工作子代理时，prompt 结尾必须包含：

```
## 收尾要求（必须执行）
1. 完成代码改动后，同步更新项目根目录的 PROJECT_SUMMARY.md：
   - 在对应章节反映本次改动
   - 在「变更日志」表格顶部添加一条记录（格式：| 日期 | 改动摘要 | 涉及文件 | 盈利影响 |）
2. 返回给我：改动摘要（3-5 句话）+ 涉及的文件列表
```
