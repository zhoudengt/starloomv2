---
description: StarLoom 本地用 venv+Homebrew Python；服务器用 Docker；禁止系统 Python 3.9；改 .env 后重启后端
alwaysApply: true
---

# StarLoom：本地运行与 Agent 行为约定

## 部署分工（固定）

- **本机开发**：默认 **不用 Docker**。后端用 `backend/.venv` + `uvicorn`；前端 `npm run dev`（Vite）。MySQL/Redis 用本机安装或仅连远程，按 `backend/.env`。
- **服务器生产**：用 **Docker**（Compose / Podman），见仓库 `docker-compose*.yml` 与 README。不要在对话里假设「本机也用 Docker 跑全套」除非用户明确说。

## Python 与虚拟环境（避免反复踩坑）

- `backend/.venv/` **被 gitignore**，克隆仓库后可能不存在；**不能**因此改用系统自带的 **`python3`（macOS Command Line Tools 常为 3.9）** 直接跑 `uvicorn`，会缺 `pydantic_settings` 等依赖。
- **创建/修复 venv**（在 `backend` 目录）：
  - macOS 优先：`/opt/homebrew/bin/python3` 或 `python3.13` / `python3.11`（须满足项目 `Python 3.11+`）。
  - 命令：`cd backend && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
- **启动后端**（必须用 venv 里的解释器/uvicorn）：
  - `cd backend && . .venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`
  - 或：`backend/.venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`
- **禁止**：在未激活 venv 时调用 PATH 里别的 `uvicorn`（例如 `~/Library/Python/3.9/...`）来跑本项目。

## 修改 `backend/.env` 或支付/配置相关代码之后

- 配置在进程启动时加载并缓存；**必须重启后端进程**才生效。
- **Agent 应主动**：在需要验证 API、或用户提到「重启后端」时：
  1. 检查 `8000` 端口是否有本项目的 uvicorn（如 `lsof -i :8000`）。
  2. 若无服务或需加载新配置：`pkill -f "uvicorn app.main:app"`（仅影响本项目典型命令行），再在 `backend` 下用 **`.venv` 内 uvicorn** 后台启动。
  3. 用 `curl -sS http://127.0.0.1:8000/health` 确认 `{"status":"ok",...}`。

## 服务器 Docker 重启（与本地区分）

- 线上重启由用户在 SSH/服务器执行（如 `docker compose restart backend`），**不要**与「本机 8000 uvicorn」混为一谈；用户说「重启服务器」时再给 Docker 命令。
