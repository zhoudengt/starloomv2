---
description: 设计审查规则 — AI Slop 检测 + 交互状态矩阵 + 移动端设计纪律（借鉴 gstack /plan-design-review 方法论）
alwaysApply: true
---

# StarLoom 设计审查规则

> 本规则在所有涉及前端 UI 改动时自动生效。写代码前先过设计关。

---

## 零、0-10 设计评分体系（借鉴 gstack /plan-design-review）

> 每个设计维度打分 0-10。低于 8 分必须修复再重评。

### 评分方法
1. 打分："信息架构：4/10"
2. 说明差距："4分因为没有定义内容层级。10分是每个页面有清晰的一级/二级/三级"
3. 修复：编辑代码补齐缺失
4. 重评："现在 8/10"
5. 循环直到 ≥8 或用户说"够了，继续"

### 7 个评分维度

| # | 维度 | 10 分标准 |
|---|------|----------|
| 1 | 信息架构 | 每个页面有清晰的一级/二级/三级内容层级 |
| 2 | 交互状态覆盖 | 每个异步组件覆盖5状态（空/加载/成功/错误/部分） |
| 3 | 用户旅程 | 有情感弧线故事板，5秒/5分钟/长期三层设计 |
| 4 | AI Slop 风险 | 零 AI 生成感，每个 UI 元素有具体设计决策 |
| 5 | 设计系统对齐 | 完全符合 StarLoom 色彩/字体/间距规范 |
| 6 | 移动端适配 | 不是"手机上堆叠"而是有意图的移动端布局 |
| 7 | 无障碍 | 键盘导航、触控目标44px、对比度达标 |

### 执行规则
- fast 子代理执行评分检查
- 评分结果上报给主模型
- <8 分维度必须修复后重评
- 所有维度 ≥8 分 = 设计审查通过

---

## 一、AI Slop 检测（每次前端改动必检）

> "AI Slop" = AI 生成代码中反复出现的平庸模式。看起来"干净"但毫无辨识度，用户潜意识会感觉"这就是模板"。

### StarLoom 高危 AI Slop 模式（发现就改）

| 编号 | 模式 | 为什么是 slop | 应该怎么做 |
|------|------|-------------|-----------|
| S1 | 渐变 hero banner + 居中大标题 + 描述文字 | 每个 AI 生成的落地页都长这样 | 用具体的星座插画/动画作为视觉锚点，标题左对齐或非对称布局 |
| S2 | 3 列等宽卡片网格 | AI 最爱的"展示多个 item"模式 | 用大小不一的卡片、横向滚动、或单列大卡+列表混合 |
| S3 | 所有元素统一圆角 (rounded-xl / rounded-2xl) | 看起来"友好"但实际是懒惰 | 按元素角色区分：按钮 rounded-full、卡片 rounded-lg、容器 rounded-none 或微妙圆角 |
| S4 | 紫色渐变背景 + 散点星星 CSS 动画 | 星座类 App 的 AI 默认审美 | 深色纯色/极微渐变 + 单一视觉亮点（如一个精致的星座图），少即是多 |
| S5 | 按钮全是"渐变底色 + 白字 + 大圆角" | 所有按钮看起来一模一样 | 主 CTA 用实色/金色，次要用描边/ghost，三级用纯文字链接 |
| S6 | icon + 标题 + 描述的三行重复卡片 | "feature list" 万能模式 | 用数据可视化、进度条、星级、具体数值代替泛泛描述 |
| S7 | 过度使用 Framer Motion（每个元素都有 fadeIn/slideUp） | 全部动，等于没动 | 只在关键转场和用户操作反馈用动画；内容区静止 |
| S8 | 完美对称的页面布局 | 真实的高端设计会有节奏变化 | 在关键区域引入不对称：图文错位、大小对比、留白不均 |

### 检测规则

- 新增或修改前端页面/组件时，Agent 应扫描代码中是否存在以上 S1-S8 模式
- 若发现 2 个以上模式同时存在，**在提交代码前先指出并提供替代方案**
- 不是说绝对不能用（如圆角按钮本身没问题），而是不能让整个页面全是同一种模式

---

## 二、交互状态矩阵（功能涉及异步/用户输入时必检）

> 每个涉及异步操作或用户输入的 UI 组件，必须覆盖以下 5 种状态。缺任何一种都是设计不完整。

### 5 状态清单

| 状态 | 含义 | StarLoom 中的典型表现 |
|------|------|---------------------|
| **空态** (Empty) | 无数据时用户看到什么 | 未选择星座、无历史报告、首次进入 |
| **加载态** (Loading) | 等待响应时用户看到什么 | SSE 流式等待首个 token、支付状态轮询、每日运势请求中 |
| **成功态** (Success) | 正常返回数据 | 运势内容展示、报告流式输出中/完成、支付成功 |
| **错误态** (Error) | 请求失败或业务异常 | LLM 超时/降级、支付失败、网络断连 |
| **部分态** (Partial) | 部分成功或过渡状态 | SSE 流式输出中途断开（已收到部分内容）、支付待确认 |

### StarLoom 关键场景状态检查表

```
页面/组件                    空态  加载  成功  错误  部分
─────────────────────────────────────────────────────
首页 12 宫格                  -    ✓    ✓    ✓    -
每日运势详情                  ✓    ✓    ✓    ✓    -
性格报告（SSE 流式）          ✓    ✓    ✓    ✓    ✓
配对分析（SSE 流式）          ✓    ✓    ✓    ✓    ✓
年度运势（SSE 流式）          ✓    ✓    ✓    ✓    ✓
支付页                       -    ✓    ✓    ✓    ✓
支付结果轮询                  -    ✓    ✓    ✓    ✓
聊天对话                     ✓    ✓    ✓    ✓    ✓
```

### 执行规则

- 新增带异步操作的页面/组件时，先列出该组件的 5 状态表，确认每种状态有对应 UI
- SSE 流式场景**必须**处理部分态（中途断开时已收到的内容要保留展示，而非清空）
- 错误态不能只是 `console.error`，必须有用户可见的提示（toast / 内联错误 / 重试按钮）
- 空态不能只是白屏或"暂无数据"四个字——要有引导性内容（如"选择你的星座开始探索"）

---

## 三、移动端设计纪律（StarLoom 特有）

### 触控目标
- 可点击元素最小尺寸 44×44px（Apple HIG 标准），星座选择卡片更大
- 按钮之间间距 >= 8px，防误触

### 字体层级（最多 4 级）
- Display: 思源宋体 24-28px（页面标题、星座名）
- Heading: 思源黑体 18-20px（区块标题）
- Body: 思源黑体 14-16px（正文内容）
- Caption: 思源黑体 12px（辅助说明、时间戳）
- 禁止出现第 5 级——如果需要更多层级，说明信息架构有问题

### 颜色使用纪律
- 金色 (#F0C75E) 只用于：主 CTA 按钮、重要高亮、星座图标描边——不泛滥
- 深紫 (#2D1B69) 用于背景和容器，不用于文字
- 正文用 white/gray-100，次要信息用 gray-400
- 每个页面最多使用 3 种功能色（主色 + 辅助色 + 强调色），不多加

### 星座主题的克制
- 星星/粒子动画最多在 1 个页面（首页）使用，其他页面保持安静
- 星座插画作为内容而非装饰——每张图要承载信息（对应具体星座），不是"氛围图"
- 宇宙/星空背景用纯深色或极微渐变 (opacity < 0.1 的叠加层)，不用肉眼明显的蓝紫渐变

---

## 四、如何使用本规则

Agent 在以下场景自动启用设计审查：

1. **新增页面**：先确认信息架构（这个页面的一级/二级/三级内容是什么），再写代码
2. **修改组件**：检查是否引入了 S1-S8 模式，若是则标出并提供替代
3. **涉及异步操作**：先列出 5 状态表，确认每种状态有 UI 方案
4. **涉及布局/样式**：检查字体层级（是否超过 4 级）、颜色使用（金色是否泛滥）、触控目标

Agent **不需要**在每次对话开头念这个规则。它作为内化的设计标准静默生效。

---

## 五、与多角色审视体系的关系

本规则是 StarLoom 多角色审视体系（`multi-role-review.mdc`）中 **R3 高级设计师** 角色的详细执行清单。当改动涉及前端 UI/样式时，多角色规则会激活本角色，按上述检查清单执行审查。
